package draw;

import model.Node;

public class naiveDraw {
    public static Node processTree(Node tree) {
        System.out.println("processing for a naiveDraw");
//        List root = tree.getRoots();
//        System.out.println(Arrays.toString(root.toArray()));
        return null;
    }
}
